# Copy to .env and add your key (never commit .env)
# Direct subscription: https://www.api-football.com/
API_FOOTBALL_KEY=your_key_here

# Set to 1 when running ./scripts/watch-scores.sh during matches to push
# score updates to GitHub every sync (keeps the online site in sync).
# PUBLISH_SCORES=1
